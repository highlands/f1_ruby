%nav.top-bar{"data-topbar" => ""}
  %ul.title-area
    %li.name
      %h1
        = link_to "Highlands SSO", root_path
    %li.toggle-topbar.menu-icon
      %a{:href => "#"}
        %span Menu
  %section.top-bar-section
    %ul.left
    / Right Nav Section
    %ul.right
      %li.divider
      - if f1_current_user
        %li= link_to 'Sign Out', destroy_f1_session_path
      - else
        %li= link_to 'Sign In', new_f1_user_session_path

